import React from 'react';
import './Node.css';
import PropTypes from 'prop-types';


const Node = (props) => {
	const style = {

	}
	return (
		<div className='Node'>

		</div>
	)
}

Node.propTypes = {
	id: PropTypes.
	color: PropTypes.string,
	size: PropTypes.number,
	x: PropTypes.number,
	y: PropTypes.number
}

export default Node;